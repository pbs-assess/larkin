---
title: "fitting"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{fitting}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(larkin)
```

```{r data}
# Data
data_ricker <- list(
  T = 100,
  R = simulation$R_4[1:100],
  S = simulation$S[1:100],
  mu_a = 2,
  sd_a = 0.5,
  min_b = 0,
  max_b = 20
)

data_larkin <- list(
  T = 100,
  R = simulation$R_4[1:100],
  S = simulation$S[1:100],
  mu_a = 2,
  sd_a = 0.5,
  min_b = rep(0, 4),
  max_b = rep(20, 4)
)

```

```{r fitting}
# Fit
# m0 <- ricker(data_ricker)
# 
# m1 <- larkin(data_larkin)


# Visualize
# rstan::plot(m1$samples)
# s1 <- shinystan::as.shinystan(m1$samples)
# shinystan::launch_shinystan(s1)

```
